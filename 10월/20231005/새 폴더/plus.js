var x, y;
var input;

input = prompt("정수를 입력하시오", "정수로");
x = parseInt(input);
// string 타입의 데이터를 int형(number)으로 변환해준다.
input = prompt("정수를 입력하시오", "정수로");
y = parseInt(input);
document.write(x + y + "<br>");
